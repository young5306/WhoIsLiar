spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url:       ${MYSQL_URL}
    username:  ${MYSQL_USERNAME}
    password:  ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  logging:
    level:
      root: INFO
      com.ssafy.backend: WARN
cookie:
  secure: true
  samesite: None

cors:
  allowed-origins:
    - https://k12a604.p.ssafy.io
    - http://localhost:5173

openvidu:
  url:    https://k12a604.p.ssafy.io:4443
  secret: YOUR_PROD_SECRET
